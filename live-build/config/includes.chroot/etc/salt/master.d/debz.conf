# debz Salt master configuration
# Enables the debz_db external pillar so nodes get their service
# definitions from state.db when they run state.highstate

ext_pillar:
  - debz_db: {}

# debz state files location
file_roots:
  base:
    - /srv/salt

pillar_roots:
  base:
    - /srv/pillar
