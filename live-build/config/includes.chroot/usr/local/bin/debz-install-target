#!/usr/bin/env bash
# Convenience forwarder — the real installer is /usr/sbin/debz-install-target.
# This exists so the tool is reachable from PATH without a full sbin lookup.
exec /usr/sbin/debz-install-target "$@"
