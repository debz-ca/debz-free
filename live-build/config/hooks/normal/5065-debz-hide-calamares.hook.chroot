#!/bin/sh
# Hide the calamares desktop entry — debz uses its own installer launcher.
set -e
if [ -f /usr/share/applications/calamares.desktop ]; then
    printf '\nNoDisplay=true\n' >> /usr/share/applications/calamares.desktop
fi
